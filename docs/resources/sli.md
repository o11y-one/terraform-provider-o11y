---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "o11y_sli Resource - O11y.one"
subcategory: ""
description: |-
  A revisioned O11y.one service-level indicator evaluated from canonical minute-level telemetry facts.
---

# o11y_sli (Resource)

A revisioned O11y.one service-level indicator evaluated from canonical minute-level telemetry facts.

## Example Usage

```terraform
resource "o11y_sli" "checkout_latency" {
  sli_key        = "checkout-latency"
  name           = "Checkout latency"
  description    = "Share of checkout server spans completed within 750ms."
  indicator_kind = "latency"
  aggregation    = "threshold_ratio"

  scope_json = jsonencode({
    service_names = ["checkout"]
    span_kinds    = ["SLI_SPAN_KIND_V1_SERVER"]
    telemetry_attribute_filters = [{
      field    = "http.request.method"
      operator = "SLI_TELEMETRY_FILTER_OPERATOR_V1_EQUALS"
      values   = ["POST"]
      source   = "SLI_TELEMETRY_ATTRIBUTE_SOURCE_V1_SPAN"
    }]
  })

  eligible_events       = "matching checkout server spans"
  good_events           = "matching spans completed within 750ms"
  excluded_events       = ""
  latency_threshold     = "750ms"
  missing_data_behavior = "unknown"
}

resource "o11y_slo" "checkout_latency" {
  slo_key         = "checkout-latency"
  name            = "Checkout latency"
  sli_id          = o11y_sli.checkout_latency.id
  sli_revision_id = o11y_sli.checkout_latency.current_revision_id
  target_ratio    = 0.95

  window_mode            = "rolling"
  rolling_window_seconds = 604800
  labels_json            = jsonencode({ service = "checkout" })
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `aggregation` (String) event_ratio or threshold_ratio.
- `indicator_kind` (String) availability, latency, agent_outcome, eval_quality, tool_success, or cost_per_successful_outcome.
- `name` (String)
- `scope_json` (String) Typed AlertScopeV1 protobuf JSON, including telemetry attribute filters and span kinds.
- `sli_key` (String) Stable immutable indicator key.

### Optional

- `description` (String)
- `eligible_events` (String) Human-readable event-set definition recorded with the SLI revision.
- `excluded_events` (String) Human-readable excluded-event definition.
- `good_events` (String) Human-readable good-event definition recorded with the SLI revision.
- `latency_threshold` (String) Go duration between 100us and 24h for latency indicators, for example 250ms or 2s.
- `missing_data_behavior` (String) unknown, good, or bad.
- `owner_display_name` (String)
- `owner_team_id` (String) Optional owner team UUID. Mutually exclusive with owner_user_id.
- `owner_user_id` (String) Optional owner user UUID. Mutually exclusive with owner_team_id.

### Read-Only

- `archived` (Boolean)
- `config_hash` (String)
- `created_at` (String)
- `current_revision_id` (String)
- `id` (String) The ID of this resource.
- `revision_number` (Number)
- `updated_at` (String)
